# Data-warehouse stack (the EL job + dbt + weekly report). Kept separate from the
# bot's requirements.txt so the bot's deploy image stays lean — only CI installs
# these. Version floors pin known-good majors for reproducible CI runs.
notion-client>=3.1
snowflake-connector-python>=4.5
dbt-snowflake>=1.11,<2
python-dotenv>=1.1
matplotlib>=3.10
requests>=2.32
